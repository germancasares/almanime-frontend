const Settings = () => {
  return (
    <main id="settings" className="container">
      <section className="column is-narrow">
        <h3 className="title">Settings</h3>
      </section>
    </main>
  );
};

export default Settings;
